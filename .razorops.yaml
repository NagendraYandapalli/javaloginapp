
tasks:
  build-deps:
    runner: razorci/openjdk:11-buster
    steps:
    - checkout
    - commands:
      - |
        mvn clean verify sonar:sonar \
          -Dsonar.projectKey=java-app \
          -Dsonar.host.url=http://localhost:9000 \
          -Dsonar.login=sqp_44cd826a5590fa2a05700653bcaf0df320a2f1f2
 

